{
  "name": "sketch",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "fragment run ./src/index.js --outDir=./dist/ --development",
    "build": "fragment build src/index.js --outDir=./dist/"
  },
  "author": "Giulian Drimba",
  "license": "ISC",
  "dependencies": {
    "events": "^3.3.0",
    "svgcanvas": "^2.5.0"
  },
  "devDependencies": {
    "fragment-tools": "^0.1.20"
  }
}
